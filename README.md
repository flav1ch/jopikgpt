# Telegram AI Bot: чат, чтение файлов и генерация файлов

Бот работает через Telegram Bot API и OpenAI-Compatible endpoint.
Поддерживает обычный чат, анализ загруженных документов/изображений и генерацию файлов по запросу.

## Возможности

### Чат

Просто напиши сообщение боту — он ответит через подключённую AI-модель.

### Чтение файлов

Поддерживаются входящие файлы:

- PDF
- DOCX
- XLSX / XLSM
- TXT
- MD
- CSV
- JSON
- YAML / XML / HTML
- файлы кода
- JPG / PNG / WebP, если твой endpoint поддерживает vision

К файлу можно добавить подпись, например:

```text
Найди риски в договоре
```

### Генерация файлов

Команда:

```text
/file docx Название документа | Напиши коммерческое предложение для юридической фирмы
```

Примеры:

```text
/file docx Коммерческое предложение | Сделай КП на разработку Telegram-бота
/file xlsx Отчет расходов | Создай таблицу расходов на месяц с категориями
/file csv Клиенты | Создай таблицу клиентов с колонками Имя, Телефон, Статус
/file json Настройки | Создай JSON-конфиг для Telegram-бота
/file md Инструкция | Напиши инструкцию по запуску проекта
/file txt Заметки | Составь список задач на неделю
```

Поддерживаемые исходящие форматы:

- TXT
- MD
- DOCX
- XLSX
- CSV
- JSON

Также бот попробует распознать естественный запрос, например:

```text
Создай файл .docx с инструкцией для новых сотрудников
```

Но надёжнее использовать `/file`.

## Установка локально

```bash
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
cp .env.example .env
python bot.py
```

## Переменные окружения

Создай `.env` локально или добавь переменные в панели хостинга:

```env
TELEGRAM_TOKEN=...
OPENAI_API_KEY=...
OPENAI_BASE_URL=https://your-custom-endpoint.com/v1
OPENAI_MODEL=your-model-name
SYSTEM_PROMPT=Ты полезный Telegram-ассистент. Отвечай на русском языке, понятно и по делу.
MAX_HISTORY_MESSAGES=20
MAX_FILE_CHARS=60000
DOWNLOADS_DIR=downloads
GENERATED_DIR=generated
```

## Запуск на хостинге

Команда запуска:

```bash
python bot.py
```

Бот использует polling, поэтому публичный webhook-домен не нужен.

## Обновление на GitHub

```bash
git add .
git commit -m "Add generated files support"
git push
```

После деплоя на хостинге обнови зависимости и перезапусти бота.

## Ограничения

- Большие загруженные файлы обрезаются по `MAX_FILE_CHARS`.
- PDF-сканы без текстового слоя не читаются без OCR.
- Изображения работают только если endpoint поддерживает vision-вход.
- XLSX создаётся через JSON-таблицу от модели. Если endpoint возвращает невалидный JSON, бот покажет ошибку и попросит повторить запрос.
- PDF-генерация не включена намеренно: на хостингах часто нужны системные библиотеки. Надёжный базовый формат для документов — DOCX.
